# LoadingSVG

Spinning loading indicator (inline SVG).

| Prop | Type | Required | Default |
| --- | --- | --- | --- |
| `customClass` | `string` | No | `'animate-spin -ml-1 mr-3 h-12 w-12 text-gray-900 dark:text-white'` |

```vue
<LoadingSVG custom-class="h-6 w-6 text-primary-600" />
```
